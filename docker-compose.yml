services:
  postgres:
    image: postgres:16-alpine
    container_name: weatheroo-db
    environment:
      POSTGRES_DB: weatheroo
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
