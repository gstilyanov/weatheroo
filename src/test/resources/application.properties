# Provide dummy values for tests so the Context can load
weather.api.key=dummy_key_for_tests
weather.api.url=http://localhost:8080
weather.sync.delay=0

# Provide a minimal city list for tests to keep them fast
weather.cities.EUROPE=London